name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-18.04
    
    steps:
    - uses: actions/checkout@v1
    - name: install 
      run: sudo ./docker/build/ci_install.sh
    - name: build
      run: ./apollo.sh build
